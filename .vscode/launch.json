{
  "configurations": [
    {
      "type": "debugpy",
      "request": "attach",
      "name": "Attach to Django",
      "host": "localhost",
      "port": 8000,
      "pathMappings": [
        {
          "localRoot": "${workspaceFolder}",
          "remoteRoot": "/backend"
        }
      ]
    }
  ]
}
