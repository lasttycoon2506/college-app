{
    "configurations": [
      {
        "type": "debugpy",
        "request": "attach",
        "name": "Attach to Django App in Docker",
        "host": "localhost",
        "port": 5678,
        "pathMappings": [
          {
            "localRoot": "${workspaceFolder}/backend",
            "remoteRoot": "/backend"
          }
        ],
        "django": true
      }
    ]
  }